import { StyleSheet } from 'react-native'

let styles

export default styles = StyleSheet.create({
    container: {
        flexDirection: 'row'
    },
    leftContainer: {
        flexDirection: 'row'
    },
    midContainer: {
        flexDirection: 'column'
    },
    avatar: {
        width: 50,
        height: 50  
    }
})